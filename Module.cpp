/*

Classes related to Math architecture

Module interprets Terms to find its derivitive

*/
#include "Module.h"
using namespace std;

Term Module::powerRule(Term inTerm) { return inTerm; }
Term Module::chainRule(Term inTerm) { return inTerm; }
Term Module::quotientRule(Term inTerm) { return inTerm; }

